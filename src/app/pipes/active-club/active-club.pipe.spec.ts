import { ActiveClubPipe } from './active-club.pipe';

describe('ActiveClubPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveClubPipe();
    expect(pipe).toBeTruthy();
  });
});
